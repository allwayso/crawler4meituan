feat: 构建组件单元测试套件

本次提交完成了 crawler 项目各核心组件的单元测试套件构建：

1. 测试方法：
   - 采用 Python 标准库 `unittest` 框架。
   - 利用 `unittest.mock` 对底层依赖（如 ADB 操作、文件读取、外部 API 调用）进行模拟，确保单元测试的独立性与执行速度。
   - 覆盖了各组件的核心功能点及异常处理场景（如 `navigator` 的异常抛出）。

2. 尚未实现的部分：
   - 整体流程测试：目前仅完成了各组件的单元测试，`workflow_manager` 的集成测试及整体自动化采集流程的端到端测试尚未确定，需在后续开发中结合实际运行环境进行设计。
   - 真实环境验证：部分组件（如 `config_manager`）的测试目前基于模拟，后续需补充针对真实配置文件读取的集成测试。
